# GoPulse 项目导读

> 本文基于仓库 `main` 分支当前实现整理，仓库版本为 `1.14.5`。

GoPulse 是一个使用 Go 与云原生思想构建的社交平台。项目本身提供用户、帖子、评论、点赞、关注、收藏、搜索、通知等常规社交能力，同时又把系统自身作为被观测对象，继续构建 Metrics、Logs、Events 的采集、传输、处理、存储和查询链路。

因此，GoPulse 的重点并不只是“实现一个社交网站”，也不只是把 MySQL、Redis、Kafka、Elasticsearch 等组件组合起来。项目真正想解决的是：当一个普通业务系统逐渐加入缓存、异步任务、搜索、可观测性、组件管理和容器化部署后，应该怎样划分组件职责、数据边界和故障边界。

# 设计思想

GoPulse 将整个系统分成两条相对独立的主线：业务系统和可观测系统。

业务系统负责产生真实业务事实。用户、帖子、评论、点赞、关注、收藏等核心状态最终以 MySQL 为准；Redis 只承担缓存职责，即使 Redis 不可用，系统仍然可以回退到 MySQL。需要异步处理的通知和搜索索引更新通过 Transactional Outbox 与 RabbitMQ 解耦，业务事务先落入 MySQL，再由后台组件可靠地把事件发送出去，避免出现“数据库写成功但消息没有发出去”的双写问题。

搜索数据存入 Elasticsearch，但 Elasticsearch 只是 MySQL 数据的查询投影。搜索索引丢失时可以根据 MySQL 中的业务事实重新构建，因此搜索能力不会反过来成为业务数据的唯一事实源。

可观测系统则负责描述“系统现在运行得怎么样”。Exporter 将 Redis、MySQL、RabbitMQ、Kafka、Elasticsearch、VictoriaMetrics 等不同组件的原始状态转换为统一的 Prometheus Metrics；Monitor 负责插件生命周期和周期采集；Message Router 负责验证消息外壳并将数据送入 Kafka；Marshaller 消费 Kafka，再进行严格校验、转换和落库。Metrics 最终写入 VictoriaMetrics，Logs 与 Events 写入 Elasticsearch，管理端通过 Backend 提供的受控接口查询这些数据。

这条链路刻意保持单一职责：

`Exporter → Monitor → Router → Kafka → Marshaller → VictoriaMetrics / Elasticsearch`

Monitor 不直接操作 Kafka，Router 不解析和清洗具体指标，Marshaller 不负责插件生命周期。组件的拆分依据是数据责任和故障边界，而不是为了把项目拆成更多服务。

RabbitMQ 与 Kafka 同时存在也是这一思想的体现。RabbitMQ 面向业务事件，强调任务消费、重试、死信和业务最终一致性；Kafka 面向可观测数据流，强调顺序消费、Offset、重放以及数据处理管道。两套消息系统承担不同职责，避免业务消息与监控数据混在同一条通道中。

项目还刻意把用户端和管理端拆成 `frontend` 与 `admin-frontend` 两个 Vue 应用。两者复用同一套 Backend 身份体系和 HttpOnly Cookie，但管理端拥有独立路由、页面和权限边界，使普通社交功能不会随着可观测和运维功能持续增长而变得耦合。

# 技术选型

| 技术 | 在 GoPulse 中的职责 | 选择原因 |
| --- | --- | --- |
| Go + Gin | Backend、Worker 以及多个可观测组件 | Go 适合长期运行的网络服务和并发任务，部署时可以生成独立二进制；Gin 用于保持 HTTP API 层简单 |
| Vue 3 + TypeScript | 用户端与管理端 | Vue 适合构建交互式 SPA，TypeScript 用于约束 API DTO、状态和组件接口，降低前后端协议变化带来的错误 |
| MySQL | 核心业务数据 | 事务能力适合用户、帖子、评论、关系等强关联数据，同时作为业务事实源 |
| Redis | Cache-Aside 缓存 | 用于降低热点读取对 MySQL 的压力，但不保存不可替代的业务事实 |
| RabbitMQ | 业务异步事件 | 适合通知、搜索索引等任务型消费场景，并且容易表达确认、重试和死信语义 |
| Elasticsearch | 全文搜索、Logs、Events | 一套存储同时覆盖全文检索和结构化日志/事件查询，适合非事务型查询数据 |
| Kafka | 可观测数据总线 | 可通过 Consumer Group、Offset 和重放能力构建稳定的数据处理管道，与业务异步任务保持隔离 |
| VictoriaMetrics | Metrics 时序存储 | 兼容 Prometheus 数据模型和查询习惯，适合保存持续产生的指标时间序列 |
| Exporter | 组件指标适配 | 把不同组件的采集方式转换成统一 Prometheus 格式，使采集层不需要理解每一种组件协议 |
| Monitor | 插件管理与采集 | 将“Exporter 怎么运行”和“什么时候采集”集中到一个受控执行层 |
| Message Router | 可观测消息入口与路由 | 固定消息边界和 Kafka Topic，生产者不直接依赖 Kafka，也不能自行决定内部路由 |
| Marshaller | 消费、校验、转换、落库 | 将传输层与数据处理层分开，保证进入存储的数据再次经过独立校验 |
| Docker Compose | 当前完整运行环境 | 当前仓库已经形成完整的容器、内部网络、非 root 运行和生命周期验证体系，适合作为现阶段产品运行基线 |
| Kubernetes | 后续部署方向 | 项目设计目标是进一步云原生化，但当前仓库的完整产品运行和验收基线仍然是 Docker/Compose，Kubernetes 尚未成为当前实现的一部分 |

# 核心数据链路

普通同步业务请求通常经过 `Browser → Frontend → Backend → MySQL / Redis`。MySQL 保存最终业务状态，Redis 提供缓存加速。

需要异步执行的业务通过 `Backend → MySQL Transaction + Outbox → RabbitMQ → Business Worker / Search Indexer` 完成。通知最终仍写回 MySQL，搜索索引则写入 Elasticsearch。这样即使 RabbitMQ、Worker 或 Elasticsearch 短暂故障，已经提交的核心业务事实仍然存在。

可观测链路从真实组件或 GoPulse 自身组件开始。Exporter 和内部 Component Metrics 暴露指标，Monitor 负责采集并包装统一消息，Router 将合法消息写入固定 Kafka Topic，Marshaller 根据消息类型进行二次校验和处理。Metrics 写入 VictoriaMetrics，Logs 与 Events 写入 Elasticsearch，最终再由 Backend 向管理员提供受权限控制的查询接口。

这种设计形成了一个完整闭环：业务组件产生数据，观测系统采集这些组件的运行状态，管理端再通过业务 Backend 查看和管理观测系统，同时任何可观测组件故障都不应该直接破坏核心社交业务。

# 仓库导读

阅读仓库时，可以先从 `VERSION` 和根目录 `README.md` 开始。`VERSION` 表示当前已经完成的产品版本，`README.md` 记录当前能力、运行方式以及主要验收入口。

`backend/` 是业务核心。这里包含 Gin HTTP API、认证授权、用户与帖子业务、Redis 缓存、Outbox、RabbitMQ Worker、搜索索引、可观测查询以及管理员接口。理解 GoPulse 的业务一致性设计，应优先阅读这一目录。

`frontend/` 是普通用户使用的社交应用，`admin-frontend/` 是独立的管理应用。前者重点关注帖子、关系、搜索和通知，后者重点关注 Metrics、Logs、Events、Exporter、告警、用户角色和审计等管理能力。

`exporters/` 中包含 Redis、MySQL、RabbitMQ、Kafka、Elasticsearch、VictoriaMetrics 等独立 Exporter。Exporter 只负责连接目标组件并生成规范指标，不负责历史存储和消息传输。

`monitor/` 是可观测系统的采集与插件执行层，负责 Exporter 生命周期、采集任务、应用日志接入和生命周期事件产生。

`router/` 是 Kafka 前面的固定传输边界。它只验证 Envelope、鉴权、选择固定 Topic 并等待 Kafka 确认，不承担具体数据转换。

`marshaller/` 是 Kafka 后面的处理层。它再次校验消息，根据 Metrics、Logs、Events 类型执行确定性的转换，并分别写入 VictoriaMetrics 或 Elasticsearch。

`componentmetrics/` 是 GoPulse 自身组件的统一运行指标库。Backend、Worker、Indexer、Monitor、Router、Marshaller 等组件通过它暴露受保护的内部 Metrics，使 GoPulse 可以观测自己的运行状态。

`deploy/` 定义当前容器化运行拓扑，包括 MySQL、Redis、RabbitMQ、Elasticsearch、Kafka、VictoriaMetrics 以及各个自研服务。`scripts/` 则包含开发、验证、故障恢复和端到端验收脚本。理解“这些组件最终怎样一起运行”，应结合这两个目录阅读。

`lifecycle/` 和 `deploy/release/` 面向 Linux 产品生命周期与发布 Bundle，处理安装、启动、检查、停止、备份和恢复等问题。它体现的是 GoPulse 从“源码项目”向“可以交付和维护的完整产品”演进的部分。

`docs/` 保存架构、插件、Events、Component Metrics、运行契约和发布相关设计文档。代码用于确认当前事实，文档用于理解设计背景，两者需要结合阅读。

# 当前项目边界

GoPulse 当前已经具备完整的业务链路、业务异步链路、搜索链路以及 Metrics / Logs / Events 可观测链路，并通过 Docker Compose 将这些组件组织成一个可以整体启动和验证的系统。

项目仍然保持明确的实验和学习属性。当前 Kafka、VictoriaMetrics 等组件主要采用单节点拓扑，可观测链路使用固定 Topic 和受控数据模型，部分生产级高可用能力没有引入。Kubernetes 是项目后续的部署目标，并不能把当前仓库描述成已经完成 Kubernetes 落地的系统。

这也是 GoPulse 的核心取舍：先把数据流、职责边界、故障恢复、权限边界和可验证性设计清楚，再继续增加集群化和高可用能力。这样项目复杂度来自真实问题，而不是单纯增加中间件数量。
