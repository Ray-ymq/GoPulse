# Router

`router` will host the message-routing component introduced in a later phase. It will route normalized events to the appropriate downstream transport or processing path.

Phase 0 keeps this directory as an architectural placeholder only. No routing rules, brokers, consumers, or producers are implemented here yet.
