# Exporters

`exporters` will host output adapters introduced in later phases. Exporters will deliver normalized data to supported observability or storage backends.

Phase 0 keeps this directory as an architectural placeholder only. No exporter plugins, network clients, or delivery logic are implemented here yet.
