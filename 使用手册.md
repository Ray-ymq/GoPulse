# GoPulse 使用手册

> 适用版本：以根目录 `VERSION` 为准，当前为 `1.9.4`。
> 适用场景：本地运行、功能体验和项目验收；不作为生产部署指南。

## 1. 系统简介

GoPulse 是一个带可观测管理能力的社区系统，当前提供两类使用入口：

| 角色 | 主要能力 |
| --- | --- |
| 普通用户 | 注册、登录、帖子浏览与发布、评论、点赞、搜索、通知 |
| 管理员 | 普通用户全部能力，以及 Metrics、Logs、Events 查询和 Redis Exporter 管理 |

主要数据链路：

- MySQL 保存用户、帖子、评论、点赞和通知等业务事实；
- Redis 缓存帖子详情，失效时自动回退 MySQL；
- RabbitMQ 承载业务异步事件，驱动通知和搜索索引；
- Elasticsearch 保存帖子搜索索引、应用日志和系统事件；
- Kafka、Marshaller 和 VictoriaMetrics 承载指标链路；
- Monitor 管理 Redis Exporter，并把指标和生命周期事件送入可观测链路。

## 2. 运行环境

推荐使用 Windows WSL2，并将仓库放在 Linux 文件系统中，例如 `~/src/GoPulse`。不要将 `/mnt/c`、`/mnt/d` 等 Windows 挂载目录作为主要工作区。

日常启动只要求：

- Git、Bash；
- Docker Engine；
- Docker Compose v2。

无需在宿主机安装 Go、Node.js、npm、MySQL、Redis 等运行环境。当前版本以 Bash 脚本为准，`scripts/*.ps1` 仅保留旧版本能力。

## 3. 快速启动

### 3.1 启动完整系统

在项目根目录执行：

```bash
./scripts/dev.sh
```

首次启动会自动：

1. 从 `.env.example` 创建 `.env`；
2. 构建当前版本的项目镜像；
3. 启动业务与可观测服务；
4. 执行数据库迁移、搜索初始化和 Kafka Topic 初始化；
5. 安装并启动受 Monitor 管理的 Redis Exporter；
6. 等待服务健康并执行基础检查。

启动成功后，容器在后台持续运行。

### 3.2 默认访问地址

| 用途 | 地址 |
| --- | --- |
| Web 应用 | <http://127.0.0.1:5173> |
| 连通性诊断页 | <http://127.0.0.1:5173/dev/status> |
| Backend 存活检查 | <http://127.0.0.1:8080/health> |
| Backend 就绪检查 | <http://127.0.0.1:8080/ready> |

默认只向本机发布 Frontend 和 Backend 端口。数据库、消息队列、搜索和可观测服务不直接暴露给浏览器或宿主网络。

### 3.3 检查与停止

只读检查当前环境：

```bash
./scripts/verify.sh
```

停止容器和网络，保留数据卷：

```bash
./scripts/down.sh
```

停止并删除默认项目的数据卷：

```bash
./scripts/down.sh --volumes --confirm-project gopulse
```

删除数据卷会清空本地账号、帖子、通知、搜索、日志、指标和插件状态，不可恢复。

## 4. 常用启动选项

```bash
# 使用指定的 Compose 项目名
./scripts/dev.sh --project-name gopulse-demo

# 使用指定环境文件
./scripts/dev.sh --env-file /path/to/development.env

# 复用已存在且与当前版本、Git revision 一致的镜像
./scripts/dev.sh --no-build
```

使用自定义项目名或环境文件后，执行 `verify.sh` 和 `down.sh` 时应传入相同参数，例如：

```bash
./scripts/verify.sh --project-name gopulse-demo
./scripts/down.sh --project-name gopulse-demo
```

不同项目名拥有独立的容器、网络和数据卷，可用于并行环境。不要让不同工作区共用同一项目名。

## 5. 本地配置

配置文件为根目录 `.env`。首次启动自动创建，也可手动创建：

```bash
cp .env.example .env
```

常用配置：

| 配置 | 作用 | 默认值 |
| --- | --- | --- |
| `PUBLISHED_HOST` | 本地发布地址 | `127.0.0.1` |
| `FRONTEND_PORT` | Web 应用端口 | `5173` |
| `HTTP_PORT` | Backend 端口 | `8080` |
| `MYSQL_*` | MySQL 数据库与账号 | 本地开发值 |
| `REDIS_*` | Redis 地址、密码和 DB | 本地开发值 |
| `AUTH_*` | 登录 Cookie、JWT 和会话设置 | 本地开发值 |

注意事项：

- 本地生命周期只允许 `PUBLISHED_HOST=127.0.0.1`；
- 修改端口后，以 `dev.sh` 最终输出的地址为准；
- `.env` 含敏感配置，不应提交到 Git；
- `.env.example` 中的密码和密钥仅供本地开发，不能用于生产环境。

## 6. 普通用户使用

### 6.1 注册与登录

1. 打开 Web 应用，进入注册页；
2. 用户名使用 3～32 位字母、数字或下划线，不区分大小写且不能重复；
3. 密码至少 8 个字符，且不超过 72 字节；
4. 注册成功后自动登录，后续可使用同一账号登录和退出。

会话保存在 HttpOnly Cookie 中。服务暂时异常时，页面可能进入认证恢复状态；服务恢复后可直接重试，不必立即清除 Cookie。

### 6.2 帖子、评论与点赞

- “帖子”页按时间从新到旧展示，使用“加载更多”分页；
- 标题长度为 1～120 个字符；
- 正文长度为 1～10000 个字符；
- 评论长度为 1～2000 个字符；
- 点赞和取消点赞是幂等操作，重复操作不会产生重复点赞记录。

当前未提供帖子编辑和删除功能。

### 6.3 搜索

在“搜索”页输入标题或正文关键词。新帖子通过 RabbitMQ 和 Search Indexer 异步写入 Elasticsearch，因此刚发布的内容可能需要短暂等待后才能搜索到。

搜索故障只影响检索功能，不会改变 MySQL 中已经保存的帖子。

### 6.4 通知

他人评论你的帖子或首次点赞时，系统会异步生成通知。通知页支持：

- 刷新最新通知；
- 跳转到关联帖子；
- 标记单条通知为已读；
- 加载更早的通知。

自己的评论或点赞、重复点赞、取消点赞不会生成新通知。通知目前不是实时推送，需要手动刷新。

## 7. 管理员使用

### 7.1 提升管理员

系统不会创建默认管理员，也不能在注册时指定管理员角色。先通过浏览器注册账号，再在默认项目运行期间执行：

```bash
backend_id=$(docker ps -q \
  --filter 'label=com.docker.compose.project=gopulse' \
  --filter 'label=com.docker.compose.service=backend')
docker exec "$backend_id" /usr/local/bin/admin-role promote --username alice
```

将 `alice` 替换为实际用户名。若使用自定义项目名，应同步替换命令中的 `gopulse`。命令可以重复执行；当前只支持提升，不支持降级、禁用、删除或浏览器角色管理。

提升后刷新页面即可看到“可观测”入口，无需重新注册账号。

### 7.2 可观测中心

管理员可访问 `/admin/observability`：

| 页面 | 功能 |
| --- | --- |
| 总览 | 查看 Redis 可用性、近期 Logs、近期 Events 和 Exporter 状态 |
| Metrics | 从固定指标目录选择指标和时间范围 |
| Logs | 按服务、模块、级别、消息、请求 ID 等条件查询应用日志 |
| Events | 按事件类型、动作、结果、错误码等条件查询系统事件 |
| Exporter | 查看、安装、启动、停止和更新 Redis Exporter |

各区域独立加载：某个可观测依赖故障时，其他区域和普通社交功能仍可能正常使用。普通用户看不到管理入口，直接访问管理路由会进入无权限页面。

## 8. 数据与运行状态

- `dev.sh` 重启不会主动删除数据卷；
- `down.sh` 默认保留数据，下次启动继续使用；
- Backend、Worker、Indexer 等无状态容器可重建；
- MySQL、Redis、RabbitMQ、Elasticsearch、Kafka、VictoriaMetrics 和 Monitor 插件状态由项目级卷或服务持久化机制维护；
- Redis 不可用时，帖子详情可回退 MySQL，但缓存能力会降级；
- Worker 或 Indexer 短暂中断时，通知或搜索会延迟，恢复后继续收敛。

## 9. 故障排查

| 现象 | 处理建议 |
| --- | --- |
| Docker 无法连接 | 启动 Docker Desktop/Engine，确认 WSL 集成正常 |
| `dev.sh` 报端口占用 | 修改 `.env` 中的 `FRONTEND_PORT` 或 `HTTP_PORT` |
| 页面无法打开 | 执行 `./scripts/verify.sh`，确认 Frontend 和 Backend 健康 |
| `/ready` 返回 `503` | 等待依赖恢复，并检查 MySQL、Redis、RabbitMQ 或 Elasticsearch |
| 新帖子暂时搜不到 | 等待 Search Indexer 完成异步索引后重试 |
| 通知迟迟未出现 | 刷新通知页，检查 RabbitMQ 和 `business-worker` |
| Metrics 无数据 | 等待采集周期，检查 Exporter、Monitor、Router、Kafka、Marshaller 和 VictoriaMetrics |
| Logs 或 Events 查询失败 | 检查 Elasticsearch、Router、Kafka 和 Marshaller |
| Exporter 操作失败 | 确认当前账号仍是管理员，并检查 Monitor 状态 |
| `--no-build` 被拒绝 | 去掉该参数，重新构建当前版本和 revision 的镜像 |

查看某个服务的近期日志，可先按项目标签找到容器：

```bash
container_id=$(docker ps -q \
  --filter 'label=com.docker.compose.project=gopulse' \
  --filter 'label=com.docker.compose.service=backend')
docker logs --tail 100 "$container_id"
```

将 `backend` 替换为 `frontend`、`business-worker`、`search-indexer`、`monitor`、`router` 或 `marshaller` 可查看对应服务。

## 10. 使用边界

当前版本主要面向本地开发与验收，尚未提供：

- 帖子编辑或删除；
- 实时通知推送；
- 浏览器用户与角色管理；
- 管理员降级、账号禁用或删除；
- 对外网络发布和完整生产部署方案；
- 当前 Redis Exporter 之外的通用多插件管理界面。

完整架构、接口、配置、开发检查和验收说明见 [README.md](README.md)。
