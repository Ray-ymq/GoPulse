# GoPulse 使用手册

> 适用于仓库 `VERSION` 标记的当前版本（编写时为 `1.13.6`）。

GoPulse 是一个带管理与可观测能力的社交应用。普通用户可以发布、搜索和互动；超级管理员可以查看运行指标、日志、事件、告警、审计记录并管理 Exporter。

## 1. 快速启动（源码工作区）

支持环境为 WSL2/Linux + Bash，需安装 Git、Docker Engine 和 Docker Compose v2。保证 Docker 有至少 2 个 CPU、6 GiB 内存和 5 GiB 可用磁盘空间。

在仓库根目录执行：

```bash
cp .env.example .env
scripts/dev.sh
```

`.env` 仅适用于本地开发；在其他环境使用前必须更换密码和密钥，且不要将该文件提交到 Git。

启动成功后访问：

| 入口 | 地址 |
| --- | --- |
| 用户端（推荐） | `http://127.0.0.1:5173` |
| Backend 健康检查 | `http://127.0.0.1:8080/health` |
| Backend 就绪检查 | `http://127.0.0.1:8080/ready` |

检查运行环境：

```bash
scripts/verify.sh
```

停止服务（默认保留数据）：

```bash
scripts/down.sh
```

如使用了自定义项目名或环境文件，启动、检查和停止时都要传入相同的 `--project-name` 与 `--env-file`。

## 2. 账号与日常使用

1. 打开用户端，选择“立即注册”。用户名限 3–32 位字母、数字或下划线；密码至少 8 个字符且不超过 72 字节。
2. 注册成功后会自动登录。会话保存在 HttpOnly Cookie 中，无需手动管理令牌。
3. 使用左侧导航访问首页、搜索、通知、收藏和个人资料。

主要功能：

- “首页”可在全部帖子与 Following 信息流之间切换。
- “发布”要求标题 1–120 个字符、正文 1–10000 个字符。作者可编辑自己的帖子，也可永久删除。
- 帖子详情页支持评论、点赞/取消点赞、收藏/取消收藏和关注作者；评论限 1–2000 个字符。
- “搜索”可按标题或正文搜索帖子，也可按用户名或显示名搜索用户。
- 个人资料可编辑显示名和个人介绍；用户名不可修改。
- 通知由后台异步生成，可能稍后到达；请手动刷新并按需标记已读。

> **注意：** 永久删除帖子无法恢复，且会一并清理相关评论、点赞和收藏。

## 3. 初始化超级管理员

新安装不提供默认管理员或默认密码。先注册普通账号，然后在同一浏览器中打开：

```text
http://127.0.0.1:5173/api/v1/users/me
```

记录返回数据中的 `id`，再在仓库根目录执行：

```bash
docker compose --project-name gopulse --env-file .env --file deploy/compose.yaml \
  --profile operations run --rm admin-role bootstrap --user-id <用户ID>
```

刷新页面后，该账号会显示“管理中心”入口。初始引导账号不可降级；不要用这条命令做日常权限管理。

管理中心包含：

- “大屏”：查看组件、关键指标、日志、事件、插件和告警概览。
- “Metrics / Logs / Events”：按时间范围和字段查询可观测数据。
- “告警”：创建、编辑、启停或删除规则，查看当前与历史告警。`unknown` 或 `stale` 不代表已恢复。
- “用户角色”：使用精确的用户 ID 查询账号，并在普通用户与超级管理员之间调整角色。
- “审计”：按操作、资源、结果和时间查询管理操作。
- “Exporter”：管理 Redis、MySQL、RabbitMQ、Kafka、Elasticsearch 和 VictoriaMetrics 官方插件的配置、连接测试、安装、启停与更新。

管理员被降级后，下一次管理请求会立即失效，但普通社交功能仍可使用。

## 4. 发布 Bundle 部署

正式产品运行路径是 Linux amd64 发布 Bundle，不需要源码仓库、Go 或 Node.js。部署前必须校验外部 `.sha256` 和 Bundle 内的 `checksums`。

将 Bundle 解压到只读目录，再准备一个独立、空的 `0700` 安装目录：

```bash
export GOPULSE_BUNDLE_DIR=/absolute/path/to/gopulse-bundle
export GOPULSE_INSTALL_DIR=/absolute/path/to/gopulse-install
mkdir -m 700 "$GOPULSE_INSTALL_DIR"
export GOPULSE_TOOL_UID="$(id -u)"
export GOPULSE_TOOL_GID="$(id -g)"
export GOPULSE_SOCKET_GID="$(stat -c %g /var/run/docker.sock)"
```

为简化后续命令，可在当前 Shell 定义：

```bash
gopulse() {
  docker compose -p gopulse-tool -f "$GOPULSE_BUNDLE_DIR/compose.yaml" run --rm -T lifecycle \
    "$@" --endpoint unix:///var/run/docker.sock --install "$GOPULSE_INSTALL_DIR"
}
```

然后依次执行：

```bash
gopulse doctor --port 18080
gopulse init --port 18080
gopulse up
gopulse verify
gopulse status
```

安装完成后访问 `http://127.0.0.1:18080`。只有边缘入口会在宿主机回环地址上发布端口。查看 Backend 最近 100 行日志或停止服务：

```bash
gopulse logs --service backend --tail 100
gopulse down
```

如需初始化 Bundle 的超级管理员，先按第 3 节取得用户 ID，再从 `gopulse status` 的 JSON 输出中记录 `project`，执行：

```bash
docker exec "$(docker ps -q --filter 'label=com.docker.compose.project=<PROJECT>' \
  --filter 'label=com.docker.compose.service=backend')" \
  /usr/local/bin/admin-role bootstrap --user-id <用户ID>
```

`down` 保留数据。所有生命周期命令都输出 JSON；其中 `state.json` 和 `secrets.json` 权限应保持为 `0600`，不得打印、提交或公开传输。详细部署约束见 [Bundle 说明](deploy/release/BUNDLE-README.md)。

## 5. 备份与恢复

使用 Bundle 生命周期工具执行一致性备份。先准备一个权限为 `0600`、内容长度为 16–4096 字节的口令文件，不要把口令放在命令参数或环境变量中。

```bash
gopulse backup --archive "$GOPULSE_INSTALL_DIR/product.gpb" \
  --passphrase-file "$GOPULSE_INSTALL_DIR/passphrase"
docker compose -p gopulse-tool -f "$GOPULSE_BUNDLE_DIR/compose.yaml" run --rm -T lifecycle \
  backup-inspect --archive "$GOPULSE_INSTALL_DIR/product.gpb" \
  --passphrase-file "$GOPULSE_INSTALL_DIR/passphrase"
```

备份会进入完整维护窗口，暂停写入并排空异步任务。请分开、安全地保管归档、口令和**完全相同的源 Bundle**。

恢复只能在同一 Bundle 创建的、已 `init` 且数据为空的新安装中执行：

```bash
gopulse restore --archive "$GOPULSE_INSTALL_DIR/product.gpb" \
  --passphrase-file "$GOPULSE_INSTALL_DIR/passphrase"
```

不要向现有部署覆盖恢复。恢复后用户、角色和业务数据保留，原 JWT 会话失效，需要重新登录。

## 6. 故障排查与数据安全

- 启动失败：先确认 Docker Engine 可用，再执行 `scripts/verify.sh`（源码工作区）或 `gopulse status`（Bundle）。
- 端口被占用：源码工作区修改 `.env` 中的 `FRONTEND_PORT`/`HTTP_PORT`；Bundle 在 `init` 时用 `--port` 选择新端口。
- 某服务不健康：源码工作区使用 `docker compose --env-file .env -f deploy/compose.yaml ps` 和指定服务的 `logs`；Bundle 使用 `gopulse logs --service <服务名> --tail 100`。
- 登录状态无法确认：在“认证恢复”页重试。网络或服务故障不会自动清除原会话。
- 搜索或通知暂未更新：这两类数据含异步处理，稍后重试或刷新。
- 管理页面返回 403：确认账号仍为 `super_admin`。角色变更会对现有会话立即生效。

清除源码工作区的所有持久化数据需要双重确认：

```bash
scripts/down.sh --volumes --confirm-project gopulse
```

> **危险：** 上述命令会删除已验证归属于该 Compose 项目的命名卷，数据不可恢复。Bundle 中对应的 `down --purge --confirm <PROJECT>` 也只应在已确认备份后执行。
