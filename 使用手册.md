# GoPulse 精简使用手册

> 适用版本：`1.3.4`（Phase 6 已完成）
>
> 当前定位：本地开发和验收环境中的社区业务系统。

## 1. 当前可用功能

- 普通用户：注册、登录、退出、浏览和发布帖子、查看详情、评论、点赞/取消点赞、搜索帖子、查看并标记通知为已读。
- 系统能力：MySQL 持久化、Redis 帖子详情缓存、RabbitMQ 异步通知、Elasticsearch 帖子搜索。
- 管理与监控：管理员身份、Redis Exporter 插件安装与启停、Redis 指标采集、服务健康检查。

当前尚未提供帖子编辑/删除、关注、实时通知推送、浏览器管理后台、多插件/多监控目标和生产级部署方案。

## 2. 环境准备

推荐在 Windows 的 WSL2 中运行，并将仓库放在 Linux 文件系统中，例如 `/home/<user>/src/GoPulse`。不要把 `/mnt/c` 或 `/mnt/d` 下的目录作为主要工作区。

需要预先安装：

- Go 1.26；
- Node.js 24 和 npm 11；
- Docker，以及 Docker Compose v2；
- Bash、Python 3、curl、flock、ps 和 sha256sum。

确认 Docker daemon 已启动后，在仓库根目录执行：

```bash
cp .env.example .env
```

`.env.example` 中的账号、密码和密钥只用于本地开发。用于其他环境时必须重新设置；生产环境还必须使用 HTTPS，并将 `AUTH_COOKIE_SECURE` 设为 `true`。

## 3. 启动、检查与停止

在仓库根目录启动完整环境：

```bash
./scripts/dev.sh
```

首次启动会安装依赖、启动 MySQL、Redis、RabbitMQ 和 Elasticsearch，执行数据库迁移，并启动 Backend、Business Worker、Search Indexer、Monitor、Redis Exporter 和 Frontend。保持该终端运行。

常用入口：

| 用途 | 地址 |
| --- | --- |
| Web 应用 | <http://localhost:5173> |
| 连通性诊断页 | <http://localhost:5173/dev/status> |
| Backend 存活检查 | <http://localhost:8080/health> |
| Backend 就绪检查 | <http://localhost:8080/ready> |
| Redis Exporter 存活检查 | <http://localhost:9121/health> |
| Redis Prometheus 指标 | <http://localhost:9121/metrics> |
| RabbitMQ 管理页 | <http://localhost:15672> |

在另一个终端执行只读检查：

```bash
./scripts/verify.sh
```

停止前台应用可在 `dev.sh` 所在终端按 `Ctrl+C`。需要同时关闭本项目的 Compose 服务时执行：

```bash
./scripts/down.sh
```

`down.sh` 会保留 MySQL、Redis、RabbitMQ 和 Elasticsearch 的命名卷，因此下次启动仍可使用已有数据。

## 4. 普通用户操作

### 注册与登录

1. 打开 <http://localhost:5173>，进入注册页。
2. 用户名填写 3～32 位字母、数字或下划线；用户名不区分大小写且不能重复。
3. 密码至少 8 个字符，且不超过 72 字节。
4. 注册成功后会自动登录。以后可从登录页使用同一账号进入系统。

登录会话保存在 HttpOnly Cookie 中。服务暂时异常时，页面可能进入“暂时无法确认登录状态”；此时会话不会被主动清除，可在服务恢复后直接重试。

### 帖子、评论与点赞

- “帖子”页按时间从新到旧显示内容，使用“加载更多”继续浏览。
- 点击“发布”创建帖子：标题为 1～120 个字符，正文为 1～10000 个字符。
- 点击帖子进入详情页，可发表评论、点赞或取消点赞；评论为 1～2000 个字符。
- 点赞和取消点赞可以安全重复操作，最终状态以页面刷新后的结果为准。

### 搜索

进入“搜索”页，输入 1～200 个字符的标题或正文关键词。新帖子通过后台任务进入搜索索引，刚发布后可能需要短暂等待；搜索服务临时不可用时，可按页面提示重试。

### 通知

其他用户评论你的帖子或首次点赞时，系统会异步生成通知。进入“通知”页后：

- 点击“刷新”获取最新通知；
- 点击“查看帖子”回到对应详情；
- 点击“标记已读”更新单条通知状态；
- 使用“加载更多”查看更早的通知。

通知目前不会实时推送，也没有未读角标。自己的评论/点赞、重复点赞或取消点赞不会产生新通知。

## 5. 管理员与 Redis Exporter

所有新注册账号默认都是普通用户，系统不会自动创建管理员。先通过浏览器注册账号，再在已加载 `.env` 的终端中提升角色：

```bash
set -a
. ./.env
set +a
(cd backend && go run ./cmd/admin-role promote --username alice)
```

把 `alice` 替换为真实用户名。命令可重复执行，但当前版本不支持降级、禁用或删除管理员，也没有浏览器管理界面。

正常执行 `dev.sh` 时，系统会自动构建、安装或更新与根目录 `VERSION` 一致的 Redis Exporter，并保持其运行。管理员如需通过公共 API 查询或控制插件，可先登录并保存 Cookie：

```bash
curl --show-error --fail-with-body \
  -c /tmp/gopulse-cookie.txt \
  -H 'Content-Type: application/json' \
  -d '{"username":"alice","password":"your-password"}' \
  http://localhost:8080/api/v1/auth/login

curl --show-error --fail-with-body \
  -b /tmp/gopulse-cookie.txt \
  http://localhost:8080/api/v1/exporter-plugins
```

Redis Exporter 当前只支持单个本地 Redis 目标。默认每 15 秒采集一次；未配置 `MONITOR_ROUTER_URL` 时，Monitor 仍会采集并构造标准消息，但不会将指标消息持久化或发送到外部服务。

## 6. 常见问题

- 页面无法打开：确认 `dev.sh` 仍在运行，再执行 `./scripts/verify.sh`。
- `/ready` 返回 `503`：查看响应中 MySQL、Redis、RabbitMQ 的状态，并确认对应容器健康。
- 搜索提示不可用：确认 Elasticsearch 和 Search Indexer 正常；必要时可在 `backend/` 下执行 `go run ./cmd/search-reindex` 重建索引。
- 通知迟迟未出现：通知是异步的，请先刷新；随后确认 RabbitMQ 和 Business Worker 正常。
- `/metrics` 返回 `503`：Exporter 进程仍可能存活，但 Redis 目标不可用或认证失败；先检查 Redis 状态和 `.env` 配置。
- PowerShell 脚本：现有 `scripts/*.ps1` 仅保留在 `0.2.1` 能力基线，当前版本请使用 WSL2 和 Bash 脚本。

更完整的接口、配置、验收和可靠性说明见 [README.md](README.md)。
