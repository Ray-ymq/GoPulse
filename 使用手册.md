# GoPulse 使用手册

> 适用版本：以根目录 `VERSION` 为准，当前为 `1.9.4`。
> 本手册用于本地运行、功能体验和项目验收，不是生产部署指南。

## 1. GoPulse 是做什么的

GoPulse 是一个可在本机一键启动的社区网站，同时带有管理员运行监控后台（下文简称“可观测后台”）。

- 普通用户可以注册、登录、发帖、评论、点赞、搜索和查看通知；
- 管理员还可以查看指标、日志、系统事件，并管理 Redis Exporter；
- 开发者可以用它检查业务功能，以及消息、搜索、缓存等模块在正常或故障时的表现。

简单理解：前台是一套社区产品，后台是一套系统运行观察工具。

| 你是谁 | 可以用它做什么 |
| --- | --- |
| 普通用户 | 体验从注册到互动、搜索、通知的完整社区流程 |
| 管理员 | 查看系统是否健康、哪里报错，以及管理指标采集插件 |
| 开发或验收人员 | 一键启动完整环境，检查功能、依赖和数据链路 |

## 2. 五分钟开始使用

### 准备环境

推荐在 Windows WSL2 中使用，并把仓库放在 Linux 文件系统，例如 `~/src/GoPulse`。不要把 `/mnt/c`、`/mnt/d` 作为主要工作区。

只需准备：

- Git 和 Bash；
- Docker Engine；
- Docker Compose v2。

无需在宿主机安装 Go、Node.js、MySQL、Redis 等软件。当前版本只维护 Bash 脚本，`scripts/*.ps1` 是旧版本留档。

### 启动系统

在项目根目录执行：

```bash
./scripts/dev.sh
```

首次启动会自动创建本地配置、构建镜像、启动全部服务、初始化数据结构，并等待服务就绪。完成后容器会在后台运行。

打开 <http://127.0.0.1:5173>，注册一个账号，就可以开始发帖和互动。

### 常用地址

| 地址 | 用途 |
| --- | --- |
| <http://127.0.0.1:5173> | Web 应用 |
| <http://127.0.0.1:5173/dev/status> | 前后端连通性诊断 |
| <http://127.0.0.1:8080/health> | Backend 是否存活 |
| <http://127.0.0.1:8080/ready> | Backend 及关键依赖是否就绪 |

数据库、消息队列和可观测服务默认不向宿主机开放端口。

## 3. 普通用户怎么用

| 功能 | 怎么用 | 需要知道 |
| --- | --- | --- |
| 注册与登录 | 在注册页创建账号，注册后自动登录 | 用户名为 3～32 位字母、数字或下划线；密码至少 8 个字符、最多 72 字节 |
| 浏览帖子 | 在“帖子”页浏览，点击“加载更多”继续查看 | 帖子按时间从新到旧排列 |
| 发布帖子 | 填写标题和正文后发布 | 标题 1～120 字，正文 1～10000 字 |
| 评论 | 进入帖子详情后发表评论 | 评论 1～2000 字 |
| 点赞 | 在帖子详情中点赞或取消点赞 | 重复点击不会产生重复点赞 |
| 搜索 | 在“搜索”页输入标题或正文关键词 | 新帖子需要短暂等待，之后才能被搜到 |
| 通知 | 在“通知”页刷新、跳转或标记已读 | 他人首次点赞或评论会产生通知；目前需要手动刷新 |

自己的点赞和评论不会通知自己。重复点赞和取消点赞也不会新增通知。

当前不支持编辑或删除帖子。

## 4. 管理员怎么用

### 创建管理员

系统没有默认管理员。先在网页注册普通账号，再执行：

```bash
backend_id=$(docker ps -q \
  --filter 'label=com.docker.compose.project=gopulse' \
  --filter 'label=com.docker.compose.service=backend')
docker exec "$backend_id" /usr/local/bin/admin-role promote --username alice
```

把 `alice` 换成实际用户名。若启动时使用了其他项目名，也要把命令中的 `gopulse` 换成该项目名。

命令可重复执行。提升成功后刷新网页，即可看到“可观测”入口。当前只能提升管理员，不能降级、禁用或删除账号。

### 可观测后台

管理员可打开 `/admin/observability`：

| 页面 | 能看到或操作什么 |
| --- | --- |
| 总览 | Redis 状态、近期日志、近期事件和 Exporter 状态 |
| Metrics | 按指标和时间范围查看系统数据 |
| Logs | 按服务、模块、级别、消息或请求 ID 查询日志 |
| Events | 查询插件启动、停止、失败和恢复等系统事件 |
| Exporter | 查看、安装、启动、停止或更新 Redis Exporter |

普通用户看不到该入口，直接访问也会进入无权限页面。某个可观测服务故障时，其他页面和社区功能仍可能正常工作。

## 5. 日常运行命令

### 检查系统

```bash
./scripts/verify.sh
```

该命令只读取运行状态，不修改业务数据。

### 停止系统并保留数据

```bash
./scripts/down.sh
```

下次启动后，原有账号、帖子和其他数据仍然存在。

### 停止系统并清空数据

```bash
./scripts/down.sh --volumes --confirm-project gopulse
```

这会永久删除默认项目的账号、帖子、通知、搜索、日志、指标和插件状态，无法恢复。

### 启动多个独立环境

```bash
./scripts/dev.sh --project-name gopulse-demo
./scripts/verify.sh --project-name gopulse-demo
./scripts/down.sh --project-name gopulse-demo
```

不同项目名拥有独立的容器、网络和数据卷。后续检查和停止时必须使用同一个项目名。

其他常用选项：

```bash
# 使用指定配置文件
./scripts/dev.sh --env-file /path/to/development.env

# 复用与当前版本和 Git revision 一致的已有镜像
./scripts/dev.sh --no-build
```

使用了 `--env-file` 后，检查和停止时也要传入同一个配置文件。

## 6. 本地配置

配置文件是根目录 `.env`。首次启动会从 `.env.example` 自动创建，也可以手动执行：

```bash
cp .env.example .env
```

| 配置 | 作用 | 默认值 |
| --- | --- | --- |
| `PUBLISHED_HOST` | 服务发布到哪个本机地址 | `127.0.0.1` |
| `FRONTEND_PORT` | Web 应用端口 | `5173` |
| `HTTP_PORT` | Backend 端口 | `8080` |
| `MYSQL_*` | 数据库连接和账号 | 本地开发值 |
| `REDIS_*` | 缓存连接、密码和 DB | 本地开发值 |
| `AUTH_*` | 登录 Cookie、JWT 和会话设置 | 本地开发值 |

注意：

- 本地脚本只允许 `PUBLISHED_HOST=127.0.0.1`；
- 修改端口后，以 `dev.sh` 输出的地址为准；
- `.env` 含敏感信息，不要提交到 Git；
- 示例密码和密钥只供本地开发，不能用于生产环境。

## 7. 系统怎样工作

不需要理解这些组件也能使用 GoPulse。下面的信息主要帮助你判断故障发生在哪里。

1. 用户、帖子、评论、点赞和通知最终保存在 MySQL；
2. Redis 缓存帖子详情，用来加快读取；Redis 故障时会回到 MySQL；
3. RabbitMQ 传递发帖、评论和点赞事件，Worker 生成通知，Indexer 更新搜索；
4. Elasticsearch 保存帖子搜索索引、应用日志和系统事件；
5. Monitor 管理 Exporter 并采集指标；Router、Kafka 和 Marshaller 把指标、日志和事件送到 VictoriaMetrics 或 Elasticsearch。

因此，部分组件短暂故障通常只会让功能降级：

- Redis 故障：帖子仍可读取，但速度可能下降；
- Worker 故障：评论和点赞仍会保存，通知延迟出现；
- Indexer 故障：帖子仍会保存，但搜索结果延迟更新；
- 可观测组件故障：社区功能通常仍可使用。

## 8. 常见问题

| 现象 | 处理方法 |
| --- | --- |
| Docker 无法连接 | 启动 Docker Desktop 或 Docker Engine，并确认 WSL 集成正常 |
| `dev.sh` 提示端口占用 | 修改 `.env` 中的 `FRONTEND_PORT` 或 `HTTP_PORT` |
| 页面打不开 | 执行 `./scripts/verify.sh`，检查 Frontend 和 Backend |
| `/ready` 返回 `503` | 等待依赖恢复；重点检查 MySQL、Redis、RabbitMQ 和 Elasticsearch |
| 新帖子搜不到 | 稍等片刻再搜索，Indexer 写入索引是异步的 |
| 通知没有出现 | 刷新通知页，并检查 RabbitMQ 和 `business-worker` |
| Metrics 没有数据 | 等待一个采集周期，并检查 Exporter 和 Monitor |
| Logs 或 Events 查询失败 | 检查 Elasticsearch、Router、Kafka 和 Marshaller |
| Exporter 操作失败 | 确认账号仍是管理员，并检查 Monitor |
| `--no-build` 被拒绝 | 去掉该选项，重新构建当前版本镜像 |

查看某个服务最近 100 行日志：

```bash
container_id=$(docker ps -q \
  --filter 'label=com.docker.compose.project=gopulse' \
  --filter 'label=com.docker.compose.service=backend')
docker logs --tail 100 "$container_id"
```

可把 `backend` 换成 `frontend`、`business-worker`、`search-indexer`、`monitor`、`router` 或 `marshaller`。

## 9. 当前使用边界

GoPulse 当前面向本地开发、演示和验收，尚未提供：

- 帖子编辑和删除；
- 实时通知推送；
- 网页端用户和角色管理；
- 管理员降级、账号禁用或删除；
- 对外网络发布和完整生产部署方案；
- Redis Exporter 之外的通用插件管理界面。

需要架构、接口、开发检查或完整验收细节时，请查看 [README.md](README.md)。
