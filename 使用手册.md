# GoPulse 使用手册

> 适用版本：以根目录 `VERSION` 为准（当前为 `1.9.4`）。
> 本手册面向本地开发与验收环境，不作为生产部署指南。

## 1. 功能概览

GoPulse 是一个带可观测管理能力的社区系统。

- 普通用户：注册、登录、退出、浏览与发布帖子、评论、点赞/取消点赞、搜索帖子、查看通知。
- 管理员：使用可观测中心查看 Metrics、Logs、Events，并安装、启动、停止或更新 Redis Exporter。
- 系统数据由 MySQL、Redis、RabbitMQ、Elasticsearch、Kafka 和 VictoriaMetrics 等容器服务支撑；默认仅向本机开放前端和 Backend 端口。

## 2. 环境要求

推荐在 Windows WSL2 中运行，并将仓库放在 Linux 文件系统（如 `~/src/GoPulse`），不要放在 `/mnt/c` 或 `/mnt/d` 下。

主流程只需要：

- Git、Bash；
- Docker Engine；
- Docker Compose v2。

当前版本使用 Bash 脚本；`scripts/*.ps1` 仅保留旧版本能力，不作为当前版本入口。

## 3. 启动与访问

在项目根目录执行：

```bash
./scripts/dev.sh
```

首次运行会自动从 `.env.example` 创建 `.env`、构建项目镜像、启动完整容器栈并执行基础检查。默认服务在后台运行。

| 用途 | 默认地址 |
| --- | --- |
| Web 应用 | <http://127.0.0.1:5173> |
| 连通性诊断页 | <http://127.0.0.1:5173/dev/status> |
| Backend 存活检查 | <http://127.0.0.1:8080/health> |
| Backend 就绪检查 | <http://127.0.0.1:8080/ready> |

如需修改本地密码或端口，请在启动前编辑 `.env`。其中的默认凭据仅用于本地开发。

检查运行状态：

```bash
./scripts/verify.sh
```

停止服务并保留数据卷：

```bash
./scripts/down.sh
```

彻底删除当前默认项目及其数据卷：

```bash
./scripts/down.sh --volumes --confirm-project gopulse
```

> 上述删除操作会清空本地业务与可观测数据，请谨慎执行。

## 4. 普通用户操作

1. 打开 Web 应用，先注册账号；用户名为 3～32 位字母、数字或下划线，密码至少 8 个字符且不超过 72 字节。
2. 登录后可在“帖子”中分页浏览，在“发布”中创建帖子，并在详情页评论或点赞。
3. 在“搜索”中按标题或正文关键词检索。新帖子异步进入索引，发布后可能需要短暂等待。
4. 在“通知”中查看他人对自己帖子的评论和首次点赞，并可标记为已读。

当依赖服务短暂不可用时，页面会保留可恢复的登录状态并给出重试提示。

## 5. 管理员操作

系统不会自动创建管理员。请先在浏览器注册账号，再在服务运行期间执行：

```bash
backend_id=$(docker ps -q \
  --filter 'label=com.docker.compose.project=gopulse' \
  --filter 'label=com.docker.compose.service=backend')
docker exec "$backend_id" /usr/local/bin/admin-role promote --username alice
```

将 `alice` 替换为实际用户名。提升成功后，该账号可从主导航进入“可观测中心”，使用：

- **总览**：查看 Redis 可用性、近期日志、事件和 Exporter 状态；
- **Metrics**：查询固定指标与时间范围；
- **Logs / Events**：按系统提供的条件筛选并分页查看；
- **Exporter**：安装、启动、停止或更新 Redis Exporter。

当前命令只支持提升管理员，不支持降级、禁用或删除账号。

## 6. 常见问题

- 页面无法打开：确认 Docker 正常运行，然后执行 `./scripts/verify.sh`。
- `/ready` 返回 `503`：说明一个或多个依赖尚未就绪，可稍后重试并查看容器日志。
- 搜索不到新帖子：等待异步索引完成；持续失败时检查 Elasticsearch 与 `search-indexer`。
- 通知未出现：通知由 RabbitMQ 和 `business-worker` 异步生成，请刷新页面后重试。
- 可观测页面部分区域报错：各区域独立加载；检查对应的 Monitor、Router、Marshaller、Elasticsearch 或 VictoriaMetrics 服务。
- 端口被占用：修改 `.env` 中的 `FRONTEND_PORT` 或 `HTTP_PORT` 后重新启动。

更多架构、接口、配置和验收说明见 [README.md](README.md)。
