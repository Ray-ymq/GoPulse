# Marshaller

`marshaller` will host the serialization and transformation component introduced in a later phase. It will convert internal observations into the external formats required by downstream systems.

Phase 0 keeps this directory as an architectural placeholder only. No codecs, schemas, or transformation pipeline are implemented here yet.
