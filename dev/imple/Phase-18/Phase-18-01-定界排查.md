Phase-18-01 定界排查

在 GoPulse 仓库执行。本次只定位失败原因，不调整门禁、不宣布验收通过，也不启动 Phase-18-02。已执行十次三轮压测；禁止再运行完整的 verify-phase18-capacity.sh。

输入与时间上限

已有十次运行已完成只读排查：404 来自旧 corpus 的 DELETE 池交集，负载配方修复已提交 e4e7fd7；500、稳态延迟波动、Outbox 发布速度仍未定位。宿主 --preflight-only 的 problems: []，当前没有 Phase-18 受管项目。本次仅做一次新配方诊断，总时限 90 分钟，从开始准备代码和环境时计时；到时立即停止压测并清理。

依次定界

运行绑定：使用新的私有工作目录和唯一 Compose project；可沿用未修改的 2.0.1 产品镜像及候选 manifest，但须另记负载代码 commit e4e7fd7、负载二进制及新 corpus.json 的 SHA-256。不得把旧候选 revision 当成新负载版本，也不得复用 v8/v9/v10 报告。

准备诊断：最多 20 分钟实现最小的分窗、分路由状态码与 5 秒或更细的延迟摘要；500 保存响应 X-Request-ID 并与 Backend 的 request_id/error_code 对齐，禁止保存 Cookie、凭据或请求体。同时采集同窗 Backend/MySQL CPU、连接等待、慢查询或锁等待，以及 Outbox pending、claim/publish 成功与失败、重试和 last-success。采样器达不到 5 秒时记录实际间隔，不能伪报。

受管环境：允许为本次诊断重建一次确定性 recipe 和正式 reindex，收敛后切回原 baseline 配置。准备阶段最多 60 分钟；任何预检失败、配方不一致或观测数据缺失，立即停止并清理。不得复用其他 project，不得改产品参数、配方规模或验收门禁。

一次短测：只运行 2 分钟 steady 150 RPS、1 分钟 burst 300 RPS、最多 2 分钟 drain。记录 404 是否消失、500 的 request-id、同秒延迟与 Backend/MySQL 指标、Outbox 净积压和发布速率。新增短测入口须保证 finally 清理，不调用固定三轮 runner；短测结果仅为诊断证据，不生成或冒充正式 capacity.json。

环境准备或短测失败时不重试、不换种子、不另建第二个 project。异常退出也须清理本次项目，保留脱敏诊断摘要。

强制停止与输出

以下任一条件出现立即停止：定位到所查异常的直接证据；总时限 90 分钟到期；诊断准备超过 20 分钟；受管环境准备超过 60 分钟；一次短测结束；需要修改产品配置或门禁才能继续。清理本次受管项目最多再给 10 分钟；清理失败须记录残留 project 标识，不得使用全局 prune。不得循环重试。

更新 Phase-18-01-排查结论.md：分别给出 404、500、稳态延迟与 Outbox 的 已证实事实、原因或待证假设、候选与负载及 corpus 哈希、证据路径、仍缺少的最小证据。若无法定位，明确写“未定位”，不得把相关性写成因果结论。短测结束后不自动启动正式三轮；正式验收所需的按秒证据和 corpus 哈希须另行纳入容量 runner/evidence。
